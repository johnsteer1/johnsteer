{
  "name": "johnsteer-site",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp *.html *.css *.js dist/ 2>/dev/null || true && cp -r assets dist/ 2>/dev/null || true",
    "start": "python3 -m http.server 8000"
  }
}
